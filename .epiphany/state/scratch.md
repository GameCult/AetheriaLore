# Scratch

Disposable working memory for one bounded AetheriaLore Epiphany subgoal.

Current bootstrap note:

- Treat `Aetheria/` as source, not as generic prose.
- First real run should model the vault and site before editing.
- Use `.\scripts\rag\rag.ps1 stats` and targeted `query` calls, then open returned notes directly.
- Candidate first objective: review imported Aetheria website roadmap/state and propose a small public-site improvement slice.

Operator intrusion audit:

- 2026-05-05: External Codex operator wired Aquarium swarm registry support and registered this workspace as `aetheria-lore`.
- No `Aetheria/` or `site/` content was edited.
- Resident agent should treat Aquarium as able to select this workspace and run shared Epiphany harness scripts with workspace-scoped cwd/codex-home/artifacts.
